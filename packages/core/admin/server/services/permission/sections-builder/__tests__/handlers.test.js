'use strict';

// const { settings } = require('../handlers');

describe('Handlers', () => {
  test('should receive an object with the rights attributes from action', () => {
    // const action = {

    // };

    // const response = settings({ action, section: [] });
    expect(4+4).toBe(8);
  })
});